#include <stdio.h>
#include <math.h>

int main(void) {
    double x ; 
    //x = fabs(0.2);
    //x = pow(5.0,2.0);
    //x = fabs(-9.7+4.7);
    x = pow(3.0,sqrt(4.0));
    printf("%.1lf\n",x);
    return 0;
}
