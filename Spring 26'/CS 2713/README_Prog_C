Intro to C 
1. Created by Dennis Ritchie 

Data (Types , Values and Variables)
1. Expressions 
2. Types : 
    short int (%hd)
    unsigned short int (%hu)
    unsigned int (%u)
    int (%d)
    long int (%ld)
    unsigned long int (%lu)
    long long int (%lld)
    unsigned long long int (%llu)
    signed char (%c)
    unsigned char (%c)
    float (%f)
    double (%lf)
    long double (%Lf)
3.  Variables :
    a. name 
    b. type
    c. value
    d. location 
    e. scope
    f. lifetime

    Declaration - {type} {name} = {value} ;


4. Values :
    a. literal value - a value that is directly represented in the source code (e.g., 42, 'A', 3.14).
    b. variable name - 
    c. operation 
    d. function call 

Statements :
1. Expression  vs statement
    a. expression : concerned with ALU , its expressing a value 
    b. statement : concerned with CU , its expressing an action , not evaluated they are executed 
2. Types of statements :
    a. simple statement 
    b. compound statement
Extras : 
CPU : ALU (Arthimetic logic unit) { ex: types and expressions}
      CU (Control unit) {ex: statements}